# Menu-Driven Product CRUD (Java + JDBC)

This repository contains a full menu-driven Java console application
performing CRUD operations on a MySQL Product table.

Add your MySQL JDBC Driver (mysql-connector-j-8.x.x.jar) into the **lib/** folder.
